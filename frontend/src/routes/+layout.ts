// SPA mode: rendered entirely on the client, served as static files by
// exact-api. SSR-disabled keeps the build static-friendly until we have a
// reason to do otherwise.
export const ssr = false;
export const prerender = true;
